# Trust anchor for the forwarding client in outputs.conf. Keep only public CA
# certificates in this PEM store; no service or CA private key belongs here.

[sslConfig]
sslRootCAPath = $SPLUNK_HOME/etc/auth/tls/public/ca.cert.pem
